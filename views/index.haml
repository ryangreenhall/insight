%h1 Welcome to Insight!
.info
  Insight will collate status information from applications in the following environments:
:each item in config
  %ul.environments
    :each name, value in item
      %li.environments
        %a.environment{ href: '/status/' + name}=name
        
