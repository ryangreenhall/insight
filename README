Insight

Provides a dashboard that collates information from multiple deployed web application nodes into a single
view.

Insight relies on web applications exposing their configuration properties as a resource with a representation based
on the following microformat:

<dl class="property">
    <dt class="name">{property.name}</dt>
    <dd class="value">{property.value}</dd>
</dl>

<dl class="monitoredEvent">
    <dt class="name">{event.name}</dt>
    <dd class="description">{event.description}</dd>
    <dd class="value">{event.value}</dd>
</dl>





